<!DOCTYPE html>
<html>
<head>
  <title>Unti</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <!-- JavaScript Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
  <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" %>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks_track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class=" ">
<nav class="navbar navbar-expand-sm container-fluid ">
  <div class="container">
    <a class="navbar-brand" href="#"><%= t('page.project') %></a>
    <div class="collapse navbar-collapse" id="navbarScroll">
      <ul class="navbar-nav me-auto my-2  navbar-nav-scroll" style="--bs-scroll-height: 100px;">
        <li class="nav-item">
          <%= link_to t('page.home'), home_path, { :class => 'text-dark nav-link active text-light' } %>
        </li>
        <li class="nav-item">
          <%= link_to t("page.create_session"), new_session_path, { :class => 'nav-link text-dark' } %>
        </li>
        <li class="nav-item dropdown float-end">
          <%= link_to '#', class: 'nav-link dropdown-toggle', data: { "bs-toggle": "dropdown" } do %>
            <%= tag.div '', class: "flag #{I18n.locale}-flag" %>
            <%= t I18n.locale %>
          <% end %>
          <ul class="dropdown-menu">
            <% I18n.available_locales.each do |locale| %>
              <li>
                <% if I18n.locale == locale %>
                  <%= tag.span t(locale), class: "dropdown-item" %>
                <% else %>
                  <%= link_to t(locale), home_path(locale: locale), class: "dropdown-item" %>
                <% end %>
              </li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
<div class="container">
  <%= yield %>
</div>
</body>
</html>